pipeline {
    agent any

    tools {
        nodejs '14.15'
    }

    stages {
        stage('Test Client') {
            steps {
                sh 'node -v'
                sh 'npm -v'
                dir('client') {
                    sh 'npm install'
                    sh 'npm test'
                }
            }
        }

        stage('SonarQube scan') {
            steps {
                withSonarQubeEnv('demo-sonarcloud') {
                    sh "sonar-scanner -Dsonar.organization=wickylim-vs -Dsonar.projectKey=jsmovievoting -Dsonar.sources=."
                }
            }
        }
    }
}